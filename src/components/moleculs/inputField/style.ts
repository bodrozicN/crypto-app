import styled from "styled-components";

export const Wrapper = styled.div`
  width: 100%;
  position: relative;
  .icon {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    left: 1.5rem;
    font-size: 1.4rem;
    color: #fff;
  }
`;
